---
import dedent from 'dedent';
import { Markdown } from 'astro/components';
import { getProps } from '@/features/utils/getProps';
  
export interface Props {
  key: string;
}

const { key } = getProps<Props>(Astro);

---
<Markdown content={dedent(key)} />
