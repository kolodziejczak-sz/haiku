---
interface Props {
  callback: (html: string) => Promise<string> | string;
}

const { callback } = Astro.props;

const content = await Astro.slots.render('default');
const html = await callback(content);

---

<>{html ? <Fragment set:html={html} /> : <slot />}</>
