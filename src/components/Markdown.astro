---
import Markdown from '@astrojs/markdown-component';
import { getProps } from '@/features/utils/getProps';
  
export interface Props {
  text?: string;
}

const { text } = getProps<Props>(Astro);
---
<Markdown content={text} />
